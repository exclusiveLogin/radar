{
  "name": "@radar/api",
  "version": "0.0.1",
  "private": true,
  "scripts": {
    "build": "nest build",
    "dev": "nest start --watch",
    "start": "node dist/main.js",
    "migration:run": "typeorm-ts-node-commonjs migration:run -d src/data-source.ts",
    "migration:generate": "typeorm-ts-node-commonjs migration:generate -d src/data-source.ts",
    "geo:seed": "ts-node --transpile-only src/scripts/seed-geo-dataset.ts",
    "geo:regions:seed": "ts-node --transpile-only src/scripts/seed-regions-catalog.ts",
    "geo:catalog:reset": "ts-node --transpile-only src/scripts/geo-catalog/reset-cli.ts",
    "geo:catalog:import": "ts-node --transpile-only src/scripts/geo-catalog/cli.ts import",
    "geo:catalog:plan": "ts-node --transpile-only src/scripts/geo-catalog/cli.ts plan",
    "geo:features:import": "ts-node --transpile-only src/scripts/geo-features/cli.ts",
    "geo:db:plan": "ts-node --transpile-only src/scripts/geo-sync/cli.ts plan",
    "geo:db:apply": "ts-node --transpile-only src/scripts/geo-sync/cli.ts apply",
    "geo:db:dedup": "ts-node --transpile-only src/scripts/geo-dedup/cli.ts",
    "typecheck": "tsc -p tsconfig.build.json --noEmit",
    "lint": "eslint \"src/**/*.ts\" --max-warnings 0"
  },
  "dependencies": {
    "@nestjs/common": "^10.4.15",
    "@nestjs/config": "^3.3.0",
    "@nestjs/core": "^10.4.15",
    "@nestjs/platform-express": "^10.4.15",
    "@nestjs/platform-ws": "^10.4.15",
    "@nestjs/swagger": "^7.4.2",
    "@nestjs/typeorm": "^10.0.2",
    "@nestjs/websockets": "^10.4.15",
    "@radar/shared": "*",
    "@radar/observability": "*",
    "class-transformer": "^0.5.1",
    "class-validator": "^0.14.1",
    "dotenv": "^16.4.7",
    "pg": "^8.13.1",
    "reflect-metadata": "^0.2.2",
    "rxjs": "^7.8.1",
    "typeorm": "^0.3.20",
    "ws": "^8.18.0",
    "xlsx": "^0.18.5",
    "zod": "^3.24.1"
  },
  "devDependencies": {
    "@nestjs/cli": "^10.4.9",
    "@nestjs/schematics": "^10.2.3",
    "@types/express": "^5.0.0",
    "@types/node": "^22.10.2",
    "@types/pg": "^8.11.10",
    "@types/ws": "^8.5.13",
    "ts-node": "^10.9.2",
    "typescript": "^5.7.2"
  }
}

# Dev runtime: исходники и node_modules — с хоста/volume (сервис npm-ci).
FROM node:22-bookworm-slim

RUN apt-get update \
  && apt-get install -y --no-install-recommends ca-certificates \
  && rm -rf /var/lib/apt/lists/*

WORKDIR /app

CMD ["npm", "run", "dev", "-w", "@radar/api"]

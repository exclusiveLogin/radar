# DEPRECATED: сервис tiles перенесён в docker-compose.app.yml (profile app, вместе с docker:dev).
# Оставлен для обратной совместимости compose -f цепочек; SSOT — docker-compose.app.yml.

name: radar

# Пустой overlay — не дублировать сервис.
